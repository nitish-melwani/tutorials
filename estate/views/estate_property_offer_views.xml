<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="estate_property_offer_views_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">tree,form</field>
    </record>


    <!-- <record id="estate_property_offer_views_tree" model="ir.ui.view">
        <field name="name">estate.property.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree>
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="accept_offer" string="Accept" states="draft" type="object" icon="fa-check" invisible="status"/>
                <button name="refuse_offer" string="Refuse" states="draft" type="object" icon="fa-times" invisible="status"/>
                <field name="status"/>
            </tree>
        </field>
    </record> -->
    
</odoo>